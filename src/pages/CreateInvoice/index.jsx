import React from 'react';
import Invoice from '../../components/Invoice/Invoice';

const index = () => {
    return (
        <div>
            <Invoice />
        </div>
    );
};

export default index;
