import React from 'react';
import SigninForm from '../../components/Signin/SigninForm';

const Signin = () => {
    return (
        <div className="container flex justify-center items-center w-full min-h-screen">
            <SigninForm />
        </div>
    );
};

export default Signin;
